<%= form_for(@come) do |f| %>
  <% if @come.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@come.errors.count, "error") %> prohibited this come from being saved:</h2>

      <ul>
      <% @come.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.text_field :game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
