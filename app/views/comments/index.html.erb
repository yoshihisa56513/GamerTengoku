<h1><%= @game.title %>の掲示板</h1>

<ul>
<li>
  <%= form_for [@game, @game.comments.build] do |f| %>
  <%= f.text_field :content %>
  <%= f.submit %>
  <% end  %>
</li>

<li>
  <% @game.comments.each do |comment| %>
  <li><%= comment.id %><%= comment.content %></li><br>
</li>
</ul>
